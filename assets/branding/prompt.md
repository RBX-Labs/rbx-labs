Goal:
Build a premium animated visual asset for embedding inside a product/dashboard card on a website.

Required deliverables:
1. Generate the best static base image first.
2. Upscale/export it as a 4K-wide PNG.
3. Create an actual animated SVG version.
4. Animation must be real SVG/CSS animation
5. Create a static fallback PNG/WebP.
6. Give downloadable files 

Restrictions:
Use the uploaded image as the exact base. Do not regenerate it. Create a single self-contained animated SVG, similar in construction to animated_cyber_telemetry_map.svg. Embed the image inside the SVG and add subtle animated overlays. Preserve the composition. Do not return HTML, JS, ZIP, or a webpage preview. Return only the final SVG. Do not treat “animation” as another static image prompt. Keep it premium, restrained, and production-ready.